# Core dependencies
anthropic>=1.0,<2.0
parallel-web>=1.3,<2.0
# The Workflows SDK ships as `render`; `render-sdk` is now an empty shim.
render>=1.0,<2.0

# Gateway
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.0,<3.0
